
<% 
    pages = ['products', 'web', 'contact'] 
%>

    <div class="text-center">
      <% pages.each do |link| %>
        <%= link_to link.to_s.capitalize, '/' + link.to_s, :class => 'd-flex-item p-3 menu-item' %>
      <% end %>
    </div>
